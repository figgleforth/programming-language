@use 'ore/html.ore'
@use 'ore/server.ore'

Inverted_Html | Html {
	css_filter = 'invert()'
}

App | Server {
	get:// { ->
		Inverted_Html([P("Example with void HTML tags"), Hr(), Small('Expect that they don\'t have a closing /tag'), Br()])
	}
}

@start App()
