`=> 4
outer = 1

global_add { left, right;
	left + right
}

increment { value = 1;
	inner = 3

	add { value;
		increment {;
			.../global_add(outer, value) `.../ is the decorator/prefix that says to look in the global scope directly for 'global_add'
		}

		increment()
	}

	add(inner)
}

increment()
