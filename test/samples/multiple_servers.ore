Server {
    port;
    new { port = 80;
        ./port = port
    }
}

Server_A | Server {
  get://a {; "From Server A" }
}

Server_B | Server {
  get://b {; "From Server B" }
}

a = Server_A(8080)
b = Server_B(8081)

#serve_http a
#serve_http b
