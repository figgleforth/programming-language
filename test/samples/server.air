Server {
    port;
    new { port = 80;
        ./port = port
    }
}

Web_App | Server {
    get://users/:user_id/posts/:post_id { user_id, post_id;
        "User |user_id| Post |post_id|"
    }
}

app = Web_App()
#serve_http app
