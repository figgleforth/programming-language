Float {}

cd Float `Don't waste space indenting, enter the scope

MAX =  inf `constants are capitalized
MIN = -inf

abs {;} `make your declarations for Float

if some_condition
	`declarations
else
	`declarations
end `use `end` or `}`

cd .. `exit Float
