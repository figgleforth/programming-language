Number {
	numerator;
	denominator = 1
}

Server {
	port;
	routes;

	new { port = nil;
		./port = port
	}
}


assert { condition;
	condition == true
}


`Uncomment one of these to crash the program.
`assert(false)
`assert(true)
